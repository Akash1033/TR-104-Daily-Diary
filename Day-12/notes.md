# Day 12 – Controlled Components

Today I learned about controlled components.

A controlled component is an input element whose value is controlled by React state.

Whenever the user types in the input field, the state updates.

This keeps the UI and state synchronized.

Although it requires a little more code, it gives full control over the input behavior.
