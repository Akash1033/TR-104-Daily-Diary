# Day 14 – API Fetching

Today I practiced fetching data from an API.

Many applications need data from servers such as product lists, user information, or posts.

React can fetch this data using JavaScript's fetch() function or libraries like axios.

Usually the API request is placed inside useEffect so it runs when the component loads.

After receiving data, we store it in state and display it on the screen.
